# twopointO

Knowledge Graph learning system with algorithmic pattern recognition and graph traversal.

## Features
- Word learning from external APIs
- Grammar pattern recognition
- Deep graph traversal
- Algorithmic (not hardcoded) clarifying questions

## Usage
```bash
python main.py
```

Supports queries like:
- `what is X?`
- `is X Y?`
